#! /bin/bash

# Ensure hermit is loaded by the editor
eval "$("$PWD/bin/hermit" env -)"

# Tero CLI configuration
export TERO_API_ENDPOINT="http://localhost:8081/graphql"

# WorkOS Client ID for authentication
# Uncomment to use staging environment
# export TERO_WORKOS_CLIENT_ID="client_01JQCC2CJMTB8AY2JRMZXFY9R1"

# Load local overrides if they exist
[[ -f .envrc.local ]] && source .envrc.local
